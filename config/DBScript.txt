use mysql;
CREATE DATABASE `easyranking` CHARACTER SET utf8 COLLATE utf8_bin;

CREATE USER 'easyranking'@'%' IDENTIFIED BY PASSWORD '*ACBBAB38251D9F16850BC013ECFD77F4B063346A';
CREATE USER 'easyranking'@'localhost' IDENTIFIED BY PASSWORD '*ACBBAB38251D9F16850BC013ECFD77F4B063346A';
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, GRANT OPTION, REFERENCES, INDEX, ALTER, CREATE TEMPORARY TABLES, LOCK TABLES, CREATE VIEW, SHOW VIEW, CREATE ROUTINE, ALTER ROUTINE, EXECUTE, TRIGGER ON `easyranking`.* TO 'easyranking'@'%';
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, GRANT OPTION, REFERENCES, INDEX, ALTER, CREATE TEMPORARY TABLES, LOCK TABLES, CREATE VIEW, SHOW VIEW, CREATE ROUTINE, ALTER ROUTINE, EXECUTE, TRIGGER ON `easyranking`.* TO 'easyranking'@'localhost';

UPDATE user SET password=PASSWORD('Dilip123') WHERE user='easyranking';
FLUSH PRIVILEGES;

UPDATE user SET password=PASSWORD('Dilip123') WHERE user='easyranking';
FLUSH PRIVILEGES;

use `easyranking`;

CREATE TABLE `CANDIDATE` (
  `ID` bigint(20) NOT NULL AUTO_INCREMENT,
  `CANDIDATE_NAME` char(100) NOT NULL,
  `PHONE_NUMBER` char(11) ,
  `CANDIDATE_ID` char(18) ,
  `JOB` char(200) DEFAULT 'F',
  `CITY` char(200),
  `CANDIDATE_BIRTHDAY` DATE,
  `GENDER` char(1) DEFAULT 'F',
  `SELF_REMARK` varchar(300) NOT NULL,
  `WEIGHT` float(5,2) DEFAULT 0,
  `HEIGHT` float(5,2) DEFAULT 0,
  `IMAGE_FILE_PATH` varchar(500),
  PRIMARY KEY (`ID`)
);




